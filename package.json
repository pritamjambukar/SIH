{
  "name": "paimana-predictive-system",
  "version": "1.0.0",
  "private": true,
  "description": "PAIMANA Predictive Infrastructure Monitoring Layer",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
